import { LightThemeFactory } from './light-theme-factory';

describe('LightThemeFactory', () => {
  it('should create an instance', () => {
    expect(new LightThemeFactory()).toBeTruthy();
  });
});
