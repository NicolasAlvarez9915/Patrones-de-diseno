import { PurpleThemeFactory } from './purple-theme-factory';

describe('PurpleThemeFactory', () => {
  it('should create an instance', () => {
    expect(new PurpleThemeFactory()).toBeTruthy();
  });
});
